# Migration `20200815190402-init-tables`

This migration has been generated by Seonghyeon Kim at 8/16/2020, 4:04:02 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `cas-dev`.`ProductOption` (
`id` int NOT NULL  AUTO_INCREMENT,
`name` varchar(191) NOT NULL ,
`productId` int NOT NULL ,
`createdAt` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`deletedAt` datetime(3)  ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `cas-dev`.`Product` (
`id` int NOT NULL  AUTO_INCREMENT,
`name` varchar(191) NOT NULL ,
`createdAt` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`deletedAt` datetime(3)  ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `cas-dev`.`ProductTag` (
`id` int NOT NULL  AUTO_INCREMENT,
`name` varchar(191) NOT NULL ,
`createdAt` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`deletedAt` datetime(3)  ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `cas-dev`.`_ProductToProductTag` (
`A` int NOT NULL ,
`B` int NOT NULL 
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE  INDEX `productId` ON `cas-dev`.`ProductOption`(`productId`)

CREATE UNIQUE INDEX `_ProductToProductTag_AB_unique` ON `cas-dev`.`_ProductToProductTag`(`A`,`B`)

CREATE  INDEX `_ProductToProductTag_B_index` ON `cas-dev`.`_ProductToProductTag`(`B`)

ALTER TABLE `cas-dev`.`ProductOption` ADD FOREIGN KEY (`productId`) REFERENCES `cas-dev`.`Product`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `cas-dev`.`_ProductToProductTag` ADD FOREIGN KEY (`A`) REFERENCES `cas-dev`.`Product`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `cas-dev`.`_ProductToProductTag` ADD FOREIGN KEY (`B`) REFERENCES `cas-dev`.`ProductTag`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200815190402-init-tables
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,40 @@
+generator client {
+  provider        = "prisma-client-js"
+  previewFeatures = ["distinct", "aggregateApi", "middlewares", "connectOrCreate", "transactionApi"]
+}
+
+datasource db {
+  provider = "mysql"
+  url = "***"
+}
+
+model ProductOption {
+  id        Int       @default(autoincrement()) @id
+  name      String
+  productId Int
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @default(now())
+  deletedAt DateTime?
+  products  Product   @relation(fields: [productId], references: [id])
+
+  @@index([productId], name: "productId")
+}
+
+model Product {
+  id        Int             @default(autoincrement()) @id
+  name      String
+  createdAt DateTime        @default(now())
+  updatedAt DateTime        @default(now())
+  deletedAt DateTime?
+  options   ProductOption[]
+  tags      ProductTag[]    @relation(references: [id])
+}
+
+model ProductTag {
+  id        Int       @default(autoincrement()) @id
+  name      String
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @default(now())
+  deletedAt DateTime?
+  products  Product[] @relation(references: [id])
+}
```


